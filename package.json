{
  "name": "mailslot-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspaces",
    "deploy": "npm run deploy --workspace @mailslot/core"
  }
}
